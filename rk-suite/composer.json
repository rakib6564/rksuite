{
    "name": "rk/rk-suite",
    "description": "RK Suite — bundled JetEngine-style Elementor toolkit (unit test harness).",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "scripts": {
        "test": "phpunit"
    }
}
